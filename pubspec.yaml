name: app_links_module
description: "App Links Module for Dynamic Links and Universal Links."
#publish_to: 'none'
repository: https://github.com/juneflow-songdo/app_links_module

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  app_links: #@add
  flutter:
    sdk: flutter
  go_router:
  june_flow_util:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/module/app_links_module/
  uses-material-design: true